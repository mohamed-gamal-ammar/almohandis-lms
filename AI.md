# تكامل الذكاء الاصطناعي (AI Provider Abstraction)
## Al-Muhandis AI Assistant Architecture Specification

---

## 1. الواجهة الموحدة (`IAIProvider`)

توفر الواجهة `IAIProvider` تجريدًا موحدًا لخدمات الذكاء الاصطناعي:

* `chat(messages, options)`: محادثة تفاعلية مع المساعد الهندسي الذكي في سياق المحاضرة الجارية.
* `explainConcept(request)`: تبسيط المفاهيم الهندسية المعقدة (مثل التحليل الإنشائي، تصميم الخرسانة، ميكانيكا التربة، أو BIM) بحسب المستوى الأكاديمي للطالب.
* `summarizeLesson(transcript, topic)`: استخراج ملخص هندسي ومعادلات حاكمة وأهم النقاط المستفادة من المحاضرة.

---

## 2. النماذج والمزودات المدعومة

1. **Google Gemini (الافتراضي)**:
   - استخدام حزمة `@google/genai` الرسمية حصريًا من جهة الخادم (Server-Side).
   - النموذج الافتراضي: `gemini-2.5-flash` للسرعة العالية والدقة في المصطلحات الهندسية.
   - الأمان: يتم حقن `process.env.GEMINI_API_KEY` على الخادم فقط ولا يُكشف للعميل نهائيًا.

2. **OpenAI GPT-4o**:
   - مدعوم كخيار بديل مع واجهة استجابة موحدة.
