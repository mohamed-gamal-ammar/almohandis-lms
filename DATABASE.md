# مخطط قاعدة البيانات (Database Architecture & Schema)
## Al-Muhandis PostgreSQL & Prisma Specification

تستخدم منصة «المهندس» قاعدة بيانات **PostgreSQL** المدارة عبر **Prisma ORM**، مع هيكل بيانات معياري يلبي متطلبات منصات التعليم الهندسي المتقدمة.

---

## 1. النماذج الرئيسية (Core Models)

### أ. المستخدمين والهوية (Identity & Access)
* **`User`**: الحساب الأساسي، البريد الإلكتروني، كلمة المرور المشفرة، الدور الوظيفي (`UserRole`).
* **`UserProfile`**: البيانات الشخصية والمهنية، الاسم بالعربية والإنجليزية، رقم الهاتف، التخصص الهندسي (`EngineeringDiscipline`)، الجامعة، ورقم القيد بنقابة المهندسين.
* **`UserSession`**: جلسات تسجيل الدخول النشطة والتوكنز وأجهزة الاتصال.

### ب. الكورسات والمحتوى الأكاديمي (Courses & Curriculum)
* **`Course`**: الدورة التعليمية، العنوان العربي والإنجليزي، الـSlug، التخصص، المستوى الهندسي (`DifficultyLevel`)، السعر، وحالة النشر.
* **`CourseModule`**: الأبواب والوحدات التعليمية داخل الكورس مرتبة بالـ`orderIndex`.
* **`Lesson`**: المحاضرات والدروس الفردية (فيديو، PDF، اختبار، مسألة برمجية).
* **`VideoAsset`**: أصل الفيديو المستضاف لدى مزود البث (Bunny.net/Mux)، رابط HLS، إعدادات الـDRM والعلامة المائية.
* **`Attachment`**: ملفات وتصميمات CAD, Revit (BIM), PDFs، والبيانات الهندسية.

### ج. التسجيلات والمتابعة (Enrollment & Progress)
* **`Enrollment`**: اشتراك الطالب في الدورة وتاريخ الانتهاء ونسبة الإنجاز.
* **`LessonProgress`**: تتبع حضور كل محاضرة، الثواني المشاهدة، وحالة الاكتمال.

### د. المدفوعات والعمليات المالية (Orders & Transactions)
* **`PaymentOrder`**: طلب الشراء، القيمة، العملة (`EGP`, `USD`, `SAR`)، وحالة الطلب.
* **`PaymentTransaction`**: المعاملة المنفذة مع بوابة الدفع (Paymob, Stripe, Fawry).
* **`WebhookEventLog`**: سجل الأحداث الواردة من بوابات الدفع لمنع التكرار (Idempotency).

### هـ. الاختبارات والتقييمات (Assessments & Quizzes)
* **`Quiz`**: اختبار المحاضرة أو نهاية الوحدة ونسبة النجاح المطلوبة.
* **`QuizQuestion`**: الأسئلة، الخيارات، الإجابة النموذجية، والشرح الهندسي التفصيلي.
* **`QuizSubmission`**: إجابات الطالب والدرجة المحققة وتاريخ التقديم.

### و. المجتمع والمساعدات (Community & System)
* **`Review`**: تقييمات الطلاب المعتمدة.
* **`DiscussionThread` & `DiscussionReply`**: منتدى النقاش الهندسي لكل محاضرة مع المعيدين.
* **`Notification`**: إشعارات التنبيهات والدروس والشهادات.
* **`FeatureFlag`**: تخزين حالات الميزات في قاعدة البيانات للتحكم الحي.
* **`AuditLog`**: سجل مراقبة العمليات الإدارية الحساسة.

---

## 2. إدارة الترحيلات (Migrations & Seeding)

* ملف الـSchema متواجد في المسار: `/prisma/schema.prisma`
* لتوليد الـClient:
  ```bash
  npm run prisma:generate
  ```
* لتطبيق التعديلات على قاعدة البيانات:
  ```bash
  npx prisma migrate dev --name init
  ```
